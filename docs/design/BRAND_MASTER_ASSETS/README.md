# BRAND_MASTER_ASSETS

Canonical brand package location for Octus design sources.

Currently registered on this branch:

- `svg_agent_packs/` — governed logo/icon SVG agent zip packs (inspiration + fidelity; exact SVGs embed rasters).
